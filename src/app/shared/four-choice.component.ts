import { ChangeDetectionStrategy, Component, EventEmitter, Input, Output } from '@angular/core';
@Component({ selector: 'app-four-choice', template: `<div class="choices">@for (option of options; track option) { <button class="choice" [class.correct]="feedback === 'correct' && option === answer" [class.incorrect]="feedback === 'incorrect' && option === selected" [disabled]="disabled" (click)="pick(option)">{{ option }} @if (feedback === 'correct' && option === answer) { <span>✓</span> }</button> }</div>`, styleUrls: ['./answer-components.scss'], changeDetection: ChangeDetectionStrategy.OnPush, standalone: true })
export class FourChoiceComponent { @Input() options: string[] = []; @Input() disabled = false; @Input() feedback: 'correct' | 'incorrect' | '' = ''; @Input() answer = ''; @Input() selected = ''; @Output() answered = new EventEmitter<string>(); pick(value: string): void { this.answered.emit(value); } }
