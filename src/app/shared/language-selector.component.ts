import { ChangeDetectionStrategy, Component, Input } from '@angular/core';
import { IonSelect, IonSelectOption } from '@ionic/angular/standalone';
import { AppLanguage } from '../core/models';
import { TranslationService } from '../core/translation.service';

@Component({ selector: 'app-language-selector', standalone: true, imports: [IonSelect, IonSelectOption], changeDetection: ChangeDetectionStrategy.OnPush, template: `@if (compact) { <div class="language-chips" role="group" [attr.aria-label]="i18n.t('language')"><button type="button" [class.active]="i18n.language() === 'en'" (click)="select('en')">EN</button><button type="button" [class.active]="i18n.language() === 'es'" (click)="select('es')">ES</button><button type="button" [class.active]="i18n.language() === 'pt-BR'" (click)="select('pt-BR')">PT</button></div> } @else { <ion-select [value]="i18n.language()" interface="popover" [attr.aria-label]="i18n.t('language')" (ionChange)="select($event.detail.value)"><ion-select-option value="en">{{ i18n.t('english') }}</ion-select-option><ion-select-option value="es">{{ i18n.t('spanish') }}</ion-select-option><ion-select-option value="pt-BR">{{ i18n.t('portuguese') }}</ion-select-option></ion-select> }`, styles: [':host { display: block; } ion-select { --padding-start: 10px; --padding-end: 10px; min-height: 42px; border: 1px solid var(--line); border-radius: 12px; color: var(--ink); font-weight: 700; } .language-chips { display:flex; padding:3px; border:1px solid #e2e0e9; border-radius:10px; background:#f4f3f8; } .language-chips button { width:31px; height:26px; padding:0; border:0; border-radius:7px; background:transparent; color:#807b8d; font:800 .64rem/1 inherit; letter-spacing:.04em; } .language-chips button.active { color:#332a83; background:#fff; box-shadow:0 1px 4px #20203319; }'] })
export class LanguageSelectorComponent { @Input() compact = false; constructor(readonly i18n: TranslationService) {} select(value: AppLanguage): void { this.i18n.setLanguage(value); } }
